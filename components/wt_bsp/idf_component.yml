dependencies:
  idf:
    version: ">=5.3"
  esp_lcd_ek79007:
    version: "1.*"
    rules:
      - if: "target == esp32p4"
  esp_lcd_ili9881c:
    version: "1.*"
    rules:
      - if: "target == esp32p4"
  esp_lvgl_port:
    version: "^2"
    require: public
    rules:
      - if: "target == esp32p4"
  lvgl/lvgl:
    version: "8.4.*"
    require: public
    rules:
      - if: "target == esp32p4"
  espressif/esp_video:
    version: "*"
    rules:
      - if: "target == esp32p4"
  espressif/esp_cam_sensor:
    version: "*"
    rules:
      - if: "target == esp32p4"
  espressif/esp_sccb_intf:
    version: "*"
    rules:
      - if: "target == esp32p4"
  espressif/esp_ipa:
    version: "*"
    rules:
      - if: "target == esp32p4"
