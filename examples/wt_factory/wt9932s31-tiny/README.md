# WT9932S31-TINY 工厂示例

同时启用摄像头、SDMMC、RGB、SW2 按键和 J1 的 UVC + CDC 复合设备。
摄像头型号和引脚由 BSP 管理，应用只包含 `wt_bsp.h`。
**SD 录像默认关闭**，默认只采集并检查摄像头、检查 SD 卡，不创建录像目录或文件。

## 构建与接线

使用 ESP-IDF v6.1 及以上版本，默认摄像头为 GC2145。

```sh
WT_BSP_BOARD=WT9932S31-TINY idf.py --preview set-board
idf.py --preview menuconfig
idf.py --preview build
idf.py --preview -p /dev/ttyACM0 flash monitor
```

在 `WT BSP -> Camera sensor` 选择实际接入的 GC2145 或 OV3660。
两款传感器共用 SCCB 地址，采用编译时选择，换型号后重新构建、烧录。
更换摄像头或 SD 卡前断开两根 USB 线，彻底断电。
型号配置不匹配也会导致探测失败并亮红灯；正常识别日志中的芯片 ID 应为
GC2145 `0x2145` 或 OV3660 `0x3660`。

需要测试录像时，在 `S31 factory -> Enable SD card recording` 中启用
`CONFIG_WT_FACTORY_ENABLE_SD_RECORDING`，重新构建、烧录后生效。
关闭该项时不编译 AVI 写入器，分段时长选项也隐藏；原有工程升级后的首次构建
同样默认关闭。连续录像会占用卡容量并消耗写入寿命，建议仅在需要时启用。

- J2：USB Serial/JTAG，用于供电、烧录和日志，不选择 USB 摄像头模式。
- J1：USB OTG，连接电脑并枚举后选择 UVC + CDC，不要求打开 CDC 串口。
- SD 卡：已格式化且有剩余空间的测试卡；挂载失败不会自动格式化。
- SW2/BOOT：启用录像后控制录制，关闭录像时只记录按键日志；SW1/EN 是复位键。

## 行为与灯色

| 状态 | 摄像头与 SD | RGB | SW2 |
| --- | --- | --- | --- |
| 默认配置，启动时未连接 J1，硬件正常 | 采集后丢弃图像，检查 SD，不写录像 | 绿色 | 记录按键日志，保持绿灯 |
| 已启用录像，启动时未连接 J1，硬件正常 | 持续录制到 SD | 绿色 | 暂停录制 |
| 已启用录像，离线暂停 | 继续采集检查摄像头，停止写录像 | 蓝色 | 恢复录制 |
| J1 完成 USB 枚举 | UVC 输出；不写录像，CDC 可查询状态和读取已结束的录像 | 绿色 | 保持原离线暂停状态 |
| 摄像头探测/采集、SD 挂载/读写或按键初始化失败 | 保留诊断接口，停止故障录像写入 | 红色 | 不能覆盖错误灯色 |

启动检测期间先显示红色，检测完成后显示当前状态。USB 模式下，SD 故障也显示
红色，但摄像头正常时仍可使用 UVC。修复 SD 故障后重新启动；不支持带电换卡。
录像关闭时仍要求 SD 卡可挂载且状态正常，缺卡仍亮红灯；绿灯不代表正在录像。

**从 USB 模式恢复离线模式：先拔掉 J1，保留 J2 供电，再按 SW1/EN 复位。**
当前板卡没有 J1 VBUS 检测线，J2 供电时仅拔掉 J1 不会可靠清除 USB 枚举状态，
绿灯也不表示已恢复录像。示例不以电脑休眠或停止 USB 通信代替拔线判断。
复位后会重新检查硬件；仅在启用录像时以新文件编号开始录像。
暂停状态不跨复位保留。

## 录像文件

以下写入行为仅在启用 `CONFIG_WT_FACTORY_ENABLE_SD_RECORDING` 时发生：

- 文件位于 `/sdcard/recordings/rec_000001.avi`，为 MJPEG AVI，直接保存 JPEG 帧。
- 按已有文件的最大编号继续递增，已有 `.avi` 和 `.avi.part` 均保留，不循环覆盖。
- 默认每 60 秒或 64 MiB 结束一个分段；时长可在 `S31 factory` 中调整。
- 按键暂停或切换 USB 时，先写索引、更新帧率和长度、同步并关闭文件，再提供读取。
- 正在写入的文件带 `.avi.part` 后缀，成功结束才改为 `.avi`。
- 突然断电可能使当前分段不完整，且文件系统仍有写缓存风险；取卡前先暂停录制，
  确认蓝灯及 `Saved ...` 日志后断电。示例不修复断电留下的 `.part` 文件。
- 空间不足或 I/O 失败保留已有文件并亮红灯，不自动删除历史录像。

GC2145 为 1600×1200、标称 8 FPS；OV3660 为 640×480、标称 25 FPS。
AVI 根据采集时间计算实际平均帧率，SD 写入速度会影响帧率。
工厂复合设备的两款摄像头均使用 UVC Bulk。实测 OV3660 原等时配置在 CDC/SD
大文件读取并发时出现不完整 JPEG 帧，切换 Bulk 后通过并发回归；独立 UVC 示例
保留 OV3660 原有的等时配置。

## CDC 命令

J1 在电脑上枚举为 `Wireless-Tag Factory` 摄像头和 CDC ACM 串口。
串口波特率可用 115200，需置 DTR；命令使用 ASCII 并以换行结束。
J2 日志独立输出，避免污染 CDC 二进制录像数据。

| 命令 | 返回 |
| --- | --- |
| `ping` | `pong\n` |
| `status` | 一行 JSON：模式、`recording_enabled` 录像配置、暂停状态、摄像头/SD 状态、录制帧数、完成分段数、文件名、空闲堆 |
| `list` | 多行 `FILE 文件名 字节数`，最后 `END\n` |
| `get rec_000001.avi` | `DATA 字节数\n`，随后精确数量的文件字节，最后 `\nEND\n` |

`list/get` 仅允许 USB 模式且存储正常时使用；只读取该目录下符合录像命名的
已完成 AVI，不接受任意路径。传输中断后重新打开串口并重新发起请求。
单个客户端顺序发送命令；读取完整响应后再发下一条。
录像关闭时仍可读取已有 AVI；卡上没有录像目录时，`list` 返回空列表 `END\n`。
此时离线模式为 `monitor`，`recording_enabled=false`，录像计数保持为 0。

## 本地验证

AVI 容器测试需要主机 C 编译器和 Python Pillow，不需要连接开发板：

```sh
python tests/test_avi.py
```

测试实际调用录像写入器，再独立解析 RIFF 和索引、解码每个 JPEG，覆盖奇偶
长度帧、采集时间、文件编号安全边界对应的禁止覆盖、满索引和非法帧拒绝。
地址及未定义行为检查由编译器 sanitizer 启用。若当前 shell 的 ESP 交叉工具链
覆盖了主机 `as`，请在主机编译工具优先的 PATH 下运行。

默认配置应验证离线采集检查正常、SW2 不切换到蓝灯、录像计数保持为 0、
不创建录像目录或文件，以及 UVC/CDC 和已有录像读取正常。
开启录像后的实机验收应覆盖离线启动、SW2 暂停/恢复、分段录像读取和解码、接入 J1 后录像
计数停止、UVC 与 CDC 并行工作、拔掉 J1 后复位恢复录像，以及断电移除
SD/摄像头后的红灯诊断。
两种摄像头分别构建和验证。独立 OV3660 示例的等时 USB 传输优先在原生 USB
主机测试；此前本机 WSL USB/IP 链路会截断等时帧。该限制不应直接套用于本工厂
示例的 Bulk 配置。

## 实机验证记录

验收状态：**通过**（2026-09-21 用户确认）。下述帧率观察记录保留用于后续追踪，
不阻塞本次工厂示例验收。

2026-09-20 使用 ESP-IDF v6.1、ESP32-S31 rev 0.0 和已格式化测试卡验证。
USB 视频由 Windows 原生 DirectShow 采集，保留原始 MJPEG 包并逐帧完整解码。
下表为加入录像开关前的实测记录，对应当前启用录像后的行为。

| 项目 | 结果 |
| --- | --- |
| OV3660 离线录制及 SW2 | 绿灯录制、蓝灯暂停、再次按键恢复绿灯；暂停和接入 J1 均正常结束 AVI |
| OV3660 自动分段 | 连续完成 7 个 60 秒分段；回读其中两段，共 2988 帧完整解码，RIFF 长度和索引正确 |
| OV3660 UVC + CDC + SD 读取 | 连续 3 轮各 1000 帧均完整，25 FPS；同时 3 次读取约 19 MB 的已完成录像，哈希一致，无看门狗超时 |
| GC2145 离线录制及 SW2 | 绿灯录制、蓝灯暂停、再次按键恢复绿灯；接回 J1 后关闭文件并停止录像 |
| GC2145 自动分段及文件收尾 | 确认约 64 MiB 和 60 秒分段；回读容量分段、按键暂停和 USB 切换时结束的三段录像，共 863 帧全部解码，AVI 长度和索引正确 |
| GC2145 UVC + CDC + SD 读取 | 连续 3 轮各 200 帧均完整，1600×1200、8 FPS；同时读取约 23 MB 的已完成录像，哈希一致，无板端错误 |
| GC2145 从 SD 录像切换到 UVC | 切换后再采集 400 帧全部完整，8 FPS；同时通过 CDC 读取约 40 MB 的新录像 |
| USB 模式停止录制 | 枚举后录像计数保持不变，CDC 打开或关闭不改变模式 |
| USB 模式下的 SW2 | GC2145 采集期间按键保持绿灯，日志确认忽略按键，录像计数保持为 0 |
| 缺 SD 卡 | 用户确认红灯，CDC 报告 SD 不可用并拒绝文件读取；UVC 250 帧完整，摄像头仍可用 |
| 缺摄像头 | 用户确认红灯，CDC 报告摄像头不可用、SD 正常；128 次查询及 12 次重复探测期间空闲堆稳定，无重启 |
| 型号配置不匹配 | OV3660 实物使用 GC2145 配置时亮红灯；改回匹配配置后恢复绿灯，100 帧视频和 CDC 查询正常 |
| 拔掉 J1 后恢复录像 | 仅拔线仍保持 USB 状态；按既定操作复位后恢复 SD 录制 |

两款工厂配置、S31 独立 CDC/两款 UVC、C2 Blink、P4 UVC 共 7 项相关构建通过。
AVI 写入器的主机测试通过 ASAN/UBSAN。
OV3660 的镜头清晰度问题按用户要求未继续调整；帧完整性通过不等同于光学质量验收。

加入录像开关后，GC2145/OV3660 分别在关闭、开启录像时构建通过，P4 UVC 回归
构建及 AVI 主机测试通过。两款旧配置重新生成后均默认关闭录像；关闭时构建产物
不包含 AVI 写入器，主程序不引用录像创建、追加、收尾及目录创建/重命名函数。
开启时分段时长默认 60 秒。

默认关闭录像的 GC2145 固件已烧录验证：离线状态采样覆盖约 226 秒，摄像头和
SD 正常，SW2 事件不改变暂停状态，用户确认保持绿灯；接回 J1 后仍为绿灯。
300 帧 UVC 图像全部解码通过，396 次 CDC 查询正常，25 个已有 AVI 的文件名和
大小均未变化；回读约 6 MB 的旧录像，SHA256 与此前一致。录像帧数、分段数始终
为 0。

默认关闭录像的 OV3660 固件已确认芯片 ID `0x3660`，离线健康状态采样覆盖约
953 秒，两次 SW2 操作均保持绿灯、录像计数为 0。CDC 查询、已有 25 个 AVI
文件名及大小核对、约 6 MB 旧录像的 SHA256 校验均通过。
正式固件累计 1000 帧 UVC 图像全部解码，331 次 CDC 查询正常，录像计数始终
为 0。测试结束已恢复默认关闭录像的正式固件，视频及串口采集均已关闭。

帧率仍有一项未定位的现象：首次离线切换 USB 后，100 帧实测约 14 FPS，
此前请求 400 帧的脚本因此超时，该轮不计为通过；保留 J1 复位后的 900 帧
均约 25 FPS。随后用临时计时固件复查离线切换及重复打开视频，额外 900 帧
均完整且约 25 FPS，USB 为高速连接，397 次 CDC 查询和并发回读正常，未复现
降速。临时计时代码已清理，未针对未知原因修改生产代码，也不宣称该现象已修复。
以上帧率按主机实际接收时间计算，不以 UVC 声明或视频时间戳代替。

2026-09-22 使用 ESP-IDF v6.1，在 `/tmp` 独立副本中运行仓库 CI 脚本的完整动态
矩阵，32/32 组合通过；额外构建默认关闭录像的 OV3660 工厂配置通过。两款合并
镜像的 bootloader、分区表和应用逐字节核对通过，镜像 checksum/SHA256 校验
通过，关闭录像配置下未编译 AVI 写入器。该轮为构建和文件校验，未重新烧录设备；
P4/P4C61 CDC 仅完成构建验证，尚未实机测试。
